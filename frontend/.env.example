# Local development URL. Production Cloudflare Pages uses its same-origin /api route,
# so VITE_API_BASE_URL can be omitted in the production build.
VITE_API_BASE_URL=http://localhost:8765

# Optional production override:
# VITE_API_BASE_URL=https://api.example.com
